//3. Explore the String methods and use one or more of them
//to reconfigure two sentences into one variable.

String s = "am i doing this right?";
String c = "i don't even know";

println(s.toLowerCase()); //converts to lower case on console
println(s.toUpperCase()); //converts to upper case on console

println(c.toLowerCase());
println(c.toUpperCase());
